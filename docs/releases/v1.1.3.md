# tube-bridge v1.1.3

Corrective metadata-only release completing publication across every public distribution surface.

## Changes

- keeps the official Registry identity `io.github.TheWhiteWater/tube-bridge`;
- replaces a long Bearer placeholder in README with an unmistakable non-secret placeholder;
- prevents the container boundary scanner from treating documentation as a credential;
- preserves checksum-pinned Registry publishing through GitHub OIDC.

There are no MCP tool or runtime behavior changes.

## Distribution

- PyPI: `pip install tube-bridge==1.1.3`
- Container: `ghcr.io/thewhitewater/tube-bridge:1.1.3`
- GitHub Release: wheel, source distribution, Agent Plugin preview, and checksums
